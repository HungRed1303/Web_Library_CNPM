import BorrowBookForm from "@/components/borrow-book-form"

export default function BorrowBookPage() {
  return (
    <main className="min-h-screen bg-gradient-to-br from-black to-gray-900">
      <BorrowBookForm />
    </main>
  )
}
